import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Activity5-DELACRUZ',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
